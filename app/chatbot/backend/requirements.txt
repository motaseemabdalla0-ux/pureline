# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic[email]==2.7.4
python-multipart==0.0.9

# --- Services Platform (requests/quotations/admin) ---
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
reportlab==4.2.2

# --- RAG knowledge base ---
# Pin numpy<2: torch 2.2.2 wheels are compiled against NumPy 1.x and break at
# runtime under NumPy 2.x ("compiled using NumPy 1.x cannot be run in 2.x").
numpy==1.26.4
chromadb==0.5.23
sentence-transformers==3.3.1
# CPU-only torch keeps the image smaller and avoids CUDA pulls.
torch==2.2.2

# --- Document ingestion ---
pypdf==5.1.0
python-docx==1.1.2

# --- File watching + HTTP client for Ollama ---
watchdog==6.0.0
requests==2.32.3
