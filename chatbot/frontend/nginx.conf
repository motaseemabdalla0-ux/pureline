server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA served under /chat/ (see vite base). Fall back to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
