# Copy to .env and fill in. Never commit real secrets.
POSTGRES_PASSWORD=change-me
CORS_ORIGINS=*
# Real LLM key (optional; also switch backend main.py to llm_reply())
LLM_API_KEY=
# Cloudflare Tunnel token (from: cloudflared tunnel token pureline, or the dashboard)
TUNNEL_TOKEN=
